<div class="row">
  <h1>Much Pyramid-scheme, but good kind!</h1>
    <div class="col-xs-4 col-xs-offset-4">
      {{> images/dogetophat }}
    </div>
    <div class="col-xs-6">
        <a href="{{currentUser.referralLink}}">{{currentUser.referralLink}}</a>
        <br>
        <h3>Converts:</h3>
        {{> partials/_childTree user=user}}
        

        <h4>So excite! Much Wow!</h4>
    </div>
    <div class="col-xs-6">
      Inventory:
      <table>
        
      </table>
    </div>
</div>
