<h1>Homepage</h1>
<a href="{{currentUser.referralLink}}">{{currentUser.referralLink}}</a>
<br>
<a href="/logout">Log out</a>
<h3>Converts:</h3>
    <ul>
        {{#each currentUser.children as |child|}}
        <li>childId: {{child}}
        </li>
        {{/each}}
    </ul>
